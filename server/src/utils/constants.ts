export const Collection = {
  user: 'User',
};

export const logObject = {
  on: 'on',
  off: 'off',
};

export const ModelName = {
  user: 'Users',
};

export const ROUTE = {
  USER: {
    INITIAL: '/',
    REFRESH_TOKEN: '/refreshToken',
  },
};

export const WHITE_LIST = ['http://localhost:3000', 'https://studio.apollographql.com'];
export const COOKIE_NAME = 'cookie_nodejsfullbe';
export const __prod__ = process.env['NODE_ENV'] === 'production';
